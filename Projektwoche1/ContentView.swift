import SwiftUI
import SwiftData

struct ContentView: View {
    
    var body: some View {
        VStack {
            HomeView()
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
